basePath: /api/v1
info:
  contact: {}
  title: Ridely
  version: "1.0"
paths:
  /auth/register:
    post:
      consumes:
      - application/json
      produces:
      - application/json
      responses: {}
      summary: Register a new user
      tags:
      - auth
securityDefinitions:
  BearerAuth:
    description: 'Enter your Bearer token in the format: Bearer <token>'
    in: header
    name: Authorization
    type: apiKey
swagger: "2.0"
